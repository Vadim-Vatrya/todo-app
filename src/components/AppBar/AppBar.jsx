import Navigation from '../Navigation';

const AppBar = () => {
  return (
    <header className="header">
      <Navigation />
      {/* {isLoggedIn ? */}
      {/* <UserMenu /> */}
    </header>
  );
};

export default AppBar;
